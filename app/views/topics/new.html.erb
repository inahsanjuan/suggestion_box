<!-- new.html.erb -->
<h1>New Topic</h1>

<%= form_for :topic, url: topics_path do |f| %>
	<div>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</div>
	<div>
		<%= f.label :description %>
		<%= f.text_area :description %>
	</div>

	<%= f.submit %>
<% end %>
